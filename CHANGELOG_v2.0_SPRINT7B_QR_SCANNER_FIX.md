# NEXO Ride v2.0 Sprint-7B QR Scanner Fix

Added `/qr-scanner/` web camera scanner page and passenger home shortcut. Existing server APIs/database untouched. Camera requires HTTPS and browser/WebView BarcodeDetector support.

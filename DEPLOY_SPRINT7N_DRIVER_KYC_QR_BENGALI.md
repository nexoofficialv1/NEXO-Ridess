# Sprint-7N Deploy Note

1. আগের server folder backup নিন।
2. Latest Sprint-7N code deploy করুন।
3. live `.env` ও `data/` folder overwrite করবেন না।
4. `npm install` প্রয়োজন হলে চালান।
5. server restart করুন।
6. Test করুন:
   - `/api/health`
   - `/api/platform/driver-kyc-qr-readiness`
   - `/admin-drivers/`
   - `/admin-areas/`
   - `/admin-qr/`

# NEXO Ride v2.0 Sprint-7E – Real Dispatch + Payment Webhook Foundation

- Driver candidate dispatch rounds with accept timeout.
- Auto reassign on driver reject or timeout.
- Dispatch attempts and queue metadata.
- Admin dispatch status and cleanup endpoints.
- Razorpay webhook endpoint with signature verification and idempotency record.
- Existing app/API compatibility preserved.
- JSON fallback remains for pilot; Redis/PostgreSQL hooks are ready for 10k-driver scale.

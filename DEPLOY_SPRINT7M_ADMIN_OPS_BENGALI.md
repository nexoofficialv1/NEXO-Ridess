# Sprint-7M Deploy Note

1. Existing server backup নিন।
2. Latest ZIP code deploy করুন।
3. `.env` untouched রাখুন।
4. `data/` folder overwrite করবেন না।
5. `npm install` প্রয়োজনে চালান।
6. `npm run s7m:check` চালান।
7. `/api/health`, `/api/platform/admin-ops-readiness`, `/admin-ops/` test করুন।

const fs=require('fs'); const path=require('path'); const server=fs.readFileSync(path.join(__dirname,'..','server.js'),'utf8'); const need=['redTeamSecurityReadiness','finalCleanupReadiness','environmentFreezeReport','productionDeployCommandPack']; const miss=need.filter(x=>!server.includes(x)); if(miss.length){console.error('Missing red-team hooks:', miss.join(', ')); process.exit(1);} console.log('Sprint-7X red-team preflight OK');
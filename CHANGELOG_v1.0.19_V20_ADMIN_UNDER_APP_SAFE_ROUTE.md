# v1.0.19 V20 Admin Under App Safe Route

- Copied standalone Admin Web App to `/app/admin.html` so it opens through the same route as Passenger/Driver app.
- Added route aliases: `/app/admin.html`, `/admin/`, `/admin.html`.
- Root home now includes a safe Admin button.

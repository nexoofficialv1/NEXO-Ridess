# NEXO Ride v2.0 Sprint-4G — Movable Center Pin Map Picker

Server Update Only.

## Fixed
- Map selection now uses a fixed center pointer like ride apps.
- User moves/pans map under the pointer and confirms the exact point.
- Tap/click map recenters the pointer when SDK supports coordinates.
- Arrow buttons added for fine adjustment.
- Nearby area suggestions update below the map.
- Search result centers the pointer instead of immediately selecting the wrong place.
- Confirm button selects the current center pin as pickup/drop.

## No APK rebuild required
Deploy to SmartASP and clear app cache/reload.

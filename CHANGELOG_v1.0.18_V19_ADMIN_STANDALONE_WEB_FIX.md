# v1.0.18 V19 Admin Standalone Web Fix

- Admin panel no longer depends on Passenger/Driver app JS.
- `/admin/`, `/admin`, `/admin.html`, `/app/admin/` open the same standalone admin page.
- Added simple admin dashboard, driver approval and payout screens.
- Root page shows app/admin links.

# NEXO Ride v2.0 Sprint-5G — Color Grading + Splash Animation Polish

## Fixed
- Global high-contrast color system applied so white-on-white / dark-on-dark text is removed.
- Passenger ride details cards now use dark readable text on light cards.
- Admin dashboard metric cards now have readable text and clear Details/Edit hint.
- Standalone admin panel contrast also corrected.
- Startup splash now uses a clean CSS toto cutout instead of a full square toto image.
- Added animated road, bouncing toto cutout, loading dots, and progress loader for slow loading.

## Safe Deploy Note
Do not overwrite `data/production.env` or `data/nexo_ride_db.json`.

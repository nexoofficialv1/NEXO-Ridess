const fs=require('fs'); const path=require('path'); const root=path.join(__dirname,'..'); const server=fs.readFileSync(path.join(root,'server.js'),'utf8'); function ok(v,m){ if(!v){ console.error('FAIL:',m); process.exitCode=1; } else console.log('OK:',m); }
ok(server.includes('realDeviceQaChecklistPayload'),'real device QA payload'); ok(server.includes('/api/platform/real-device-qa-checklist'),'real device QA API'); ok(server.includes('Driver login remains active'),'trusted device checklist'); ok(server.includes('SOS event appears'),'SOS checklist'); if(process.exitCode) process.exit(process.exitCode); console.log('SPRINT8A REAL DEVICE QA PREFLIGHT PASS');

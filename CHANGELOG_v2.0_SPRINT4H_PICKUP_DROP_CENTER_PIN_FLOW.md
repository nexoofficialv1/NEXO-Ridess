# NEXO Ride v2.0 Sprint-4H — Pickup + Drop Same Center Pin Flow

Server Update Only.

## Fixed
- Pickup and Drop now both use the same movable center-pin picker.
- Confirming Pickup automatically moves the booking flow to Drop selection.
- Confirming Drop automatically moves the flow to Fare/Route preview.
- Coordinates are preserved in the selected field text for backend route/fare parsing.
- Prevents pickup selection from being treated as final when drop is still empty.

## No APK rebuild
Deploy to SmartASP and clear app cache/reload.
